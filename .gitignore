node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/
.vitest/
# Playwright test output (traces, screenshots, videos).
test-results/
# Per-developer Claude Code workspace (session worktrees, scratch files).
.claude/

# Other package managers' lockfiles. This package is managed with pnpm and
# ships a committed pnpm-lock.yaml — anything else is from a contributor running
# npm/bun/yarn locally and shouldn't be committed.
bun.lock
package-lock.json
yarn.lock
